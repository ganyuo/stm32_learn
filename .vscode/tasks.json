{
    "tasks": [
        {
            "type": "shell",
            "label": "task_build",
            "command": "make",
            "args": [],
            "options": {
                "env": {
                    "PATH": "${env:HOME}/programs/gcc-arm-none-eabi-10.3/bin:${env:PATH}"
                },
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}